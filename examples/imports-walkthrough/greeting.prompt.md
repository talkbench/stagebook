---
name: greeting
type: noResponse
---

# Hello!

You've completed the intake demographics. After clicking **Done** below,
you'll see the same demographics survey again on the exit page — the
two sets of responses get unique storage keys
(`prompt_intake_age` vs `prompt_followup_age`, etc.) thanks to the
`prefix:` field passed at each invocation.
